import { v4 as UUID } from "uuid"
import bcrypt from "bcryptjs"


export default async function seed(){

    const roles =[
        {id:'admin',name:'Administrador'},
        {id:'user',name:'Usuario escritor'},
    ]
}